zone: {{ zone }}
network: {{ network }}
vrf: {{ vrf }}
subnets:
{%- set subnet = 'pub-adm' %}
  - subnet: {{ subnet }}
    size: 29
{%- set subnet = 'pub-lb' %}
  - subnet: {{ subnet }}
    size: 29
{%- set subnet = 'pub-svc' %}
  - subnet: {{ subnet }}
    size: 28
    hosts:
      - {{ zone }}{{ subnet }}-gw
      {% for i in range(1, 3) %}
      - {{ zone }}{{ subnet }}-gw{{ '%02d'|format(i) }}
      {% endfor %}
      - {{ zone }}{{ subnet }}-ns
      - {{ zone }}{{ subnet }}-lb
      {% for i in range(1, 3) %}
      - {{ zone }}{{ subnet }}-smtp{{ '%02d'|format(i) }}
      {% endfor %}
      - {{ zone }}{{ subnet }}-apiadmin
      - {{ zone }}{{ subnet }}-apibilling
      - {{ zone }}{{ subnet }}-apiticket
      - {{ zone }}{{ subnet }}-apiimage
      - {{ zone }}{{ subnet }}-sentry
